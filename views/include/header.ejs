<nav class="navbar navbar-default navbar-static-top" role="navigation">

  <% if(alert_msg) { %>
    <div class="alert alert-warning alert-dismissible" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <strong><%= alert_msg %></strong>
    </div>
  <% } %>

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/" style="color: #347cdf">源通讯项目管理平台</a>
    </div>

    <ul class="nav navbar-nav navbar-right">
      <form class="navbar-form navbar-left" role="search" action="/search" method="post">
        <div class="form-group">
          <input type="text" class="form-control" name="search" placeholder="搜索">
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>
      <li><a href="#">通知 <span class="badge">0</span></a></li>
        <% if(req.isAuthenticated()) { %>
        <li class="ns-dropdown">
            <a href="/user/<%= req.user[0].username %>" class="ns-dropdown-toggle" data-toggle="dropdown"><%= req.user[0].username %> </a>
        </li>
        <% } else { %>
        <li>
            <a href="/login">Login</a>
        </li>
        <% } %>
    </ul>

  </div>
</nav>
